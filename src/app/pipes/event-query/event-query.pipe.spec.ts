import { EventQueryPipe } from './event-query.pipe';

describe('EventQueryPipe', () => {
  it('create an instance', () => {
    const pipe = new EventQueryPipe();
    expect(pipe).toBeTruthy();
  });
});
